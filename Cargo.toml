[package]
name = "piske"
version = "0.1.0"
authors = ["Jamie Blondin <jblondin@gmail.com>"]
build = "build.rs"

[build-dependencies]
peg = { path = "../rust-peg" }

[dependencies]
sindra = { path = "../sindra" }
regex = "0.2"
lazy_static = "0.2"
